apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: dev
spec:
  rules:
  - host: grafana.example.com
    http:
      paths:
      - backend:
          service:
            name: grafana
            port:
              name: http
        path: /
        pathType: Prefix
