apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-manager-monitor
  namespace: my-flink
spec:
  endpoints:
  - port: metrics
  jobLabel: component
  selector:
    matchLabels:
      component: taskmanager
